{
  "name": "bole-animal-feed-processing",
  "version": "1.0.0",
  "description": "Bole Animal Feed Processing - Official Website & Admin Dashboard",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/index.js",
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "setup": "npm run install:all && npm run build && echo '\n✅ Setup complete! Run: npm start\n'"
  },
  "keywords": ["animal-feed", "ethiopia", "poultry", "livestock"]
}
